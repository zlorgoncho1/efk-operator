apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: efk-logging
spec:
  template:
    spec:
      containers:
      - name: manager
        image: zlorg/efk-operator:v0.1.8

